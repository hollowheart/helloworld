# Copy to .env (do not commit). Used by teams_graph CLI / your backend.

AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=

# Delegated refresh token for the Microsoft user (store in DB in production).
AZURE_REFRESH_TOKEN=
